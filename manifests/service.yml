apiVersion: v1
kind: Service
metadata:
    name: "gaurav-akscluster"
    labels:
        app: "gaurav-akscluster"
spec:
    type: LoadBalancer
    ports:
    - port: 8080
      targetPort: http
      protocol: TCP
      name: http
    selector:
        app: "gaurav-akscluster"
