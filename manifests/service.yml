apiVersion: v1
kind: Service
metadata:
    name: "gauravakscluster"
    labels:
        app: "gauravakscluster"
spec:
    type: LoadBalancer
    ports:
    - port: 8080
      targetPort: http
      protocol: TCP
      name: http
    selector:
        app: "gauravakscluster"
